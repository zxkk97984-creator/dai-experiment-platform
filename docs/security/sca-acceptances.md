# Python SCA 临时接受记录（TASK-026）

> 规则：每一项必须有**责任人**与**到期日**；到期前完成修复（升级/替换/确认不可达），
> 否则该项接受自动失效并恢复门禁阻断。本文件随仓库走，CI 通过
> `scripts/check_sca.py` 比对 `sca-accepted.json`（本文件的机器可读清单）实施门禁——
> 新增/修改接受项时两个文件必须同步更新。

审计工具：`pip-audit -r backend/requirements.txt`（2026-08-14）。

已修复（本次任务内升级并回归，当前审计不再报出）：
- python-dotenv 1.0.1 → 1.2.2（PYSEC-2026-2270）
- python-jose 3.3.0 → 3.4.0（PYSEC-2024-232/233、PYSEC-2025-185——OSV 记录
  last_affected=3.3.0，3.4.0 即修复版本；升级后 25 个认证测试回归通过）

## 接受项

| 包 | 版本 | 公告 | 阻塞原因 | 缓解/不可达性 | 责任人 | 到期日 |
| --- | --- | --- | --- | --- | --- | --- |
| starlette | 0.41.3 | PYSEC-2026-161/248/249/1941/1942/2280/2281 | fastapi==0.115.6 依赖 pin（<0.42）；修复版本为 0.47+/1.x，需 FastAPI 大版本升级，属独立升级周期 | 平台反代（nginx）在前、应用仅内网部署；CORS/上传路径均有应用层限制 | 后端负责人 | 2026-09-30 |
| pyasn1 | 0.4.8 | PYSEC-2026-2263/3455/3456/3457 | python-jose[cryptography]==3.4.0 依赖 pin（<0.5.0）；修复版本 0.6.x 超出上游约束 | pyasn1 仅由 jose 的 RSA 密钥解析路径间接使用；平台使用对称 HMAC（JWT HS256）+ cryptography 后端，未启用 RSA 解析路径 | 后端负责人 | 2026-09-30 |
| ecdsa | 0.19.2 | PYSEC-2026-1325 | 无修复版本 | 由 jose 依赖树带入，未使用 EC 算法路径 | 后端负责人 | 2026-09-30 |
| pytest | 8.3.4 | PYSEC-2026-1845 | 修复版本 9.x 为测试框架大版本，全量迁移属独立工作项；测试时依赖，不进入运行路径 | 仅在测试/构建阶段执行；不随 API 服务运行时加载 | 测试负责人 | 2026-09-30 |

## 到期处理

到期时逐项复核：能升级则升级并回归；仍阻塞则更新日期并注明原因；
确认公告不可达（如未启用受影响代码路径）则把“不可达性证据”固化到对应测试。
