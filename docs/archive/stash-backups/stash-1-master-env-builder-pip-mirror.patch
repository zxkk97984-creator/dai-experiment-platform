diff --git a/backend/app/config.py b/backend/app/config.py
index 7e9cc96..f8d46bd 100644
--- a/backend/app/config.py
+++ b/backend/app/config.py
@@ -139,6 +139,8 @@ class Settings(BaseSettings):
     env_image_repository: str = "dai-env"
     env_base_image: str = "python:3.12-slim"
     env_build_log_max_bytes: int = Field(default=60 * 1024, ge=1024, le=1024 * 1024)
+    # 本地开发：pip 镜像源（国内网络直连 PyPI 不稳定；构建环境镜像时注入 --index-url）
+    env_pip_index_url: str | None = None
 
 
 @lru_cache
diff --git a/backend/app/services/environment_builder.py b/backend/app/services/environment_builder.py
index ba1306b..7a9533a 100644
--- a/backend/app/services/environment_builder.py
+++ b/backend/app/services/environment_builder.py
@@ -54,6 +54,7 @@ class BuildSpec:
     repository: str
     packages: tuple[PackageEntry, ...]  # 已按 pip_name 排序
     kernel_runner_source: str
+    pip_index_url: str | None = None  # 本地开发 pip 镜像源
 
     @property
     def image_tag(self) -> str:
@@ -143,6 +144,7 @@ def canonical_build_spec(
         repository=settings.env_image_repository,
         packages=tuple(entries),
         kernel_runner_source=_load_kernel_runner(),
+        pip_index_url=settings.env_pip_index_url,
     )
 
 
@@ -176,6 +178,8 @@ def render_dockerfile(spec: BuildSpec) -> str:
     - pytorch_cpu 包使用内置官方 CPU wheel index；URL 不来自请求
     - 复制可信 kernel_runner.py；创建 UID 1000 非 root 用户与 /course /work /tmp
     """
+    # 本地开发：国内网络直连 PyPI 不稳定，可通过 settings 指定镜像源（如清华 tuna）
+    _index_opt = f" --index-url {spec.pip_index_url}" if spec.pip_index_url else ""
     lines = [
         "# 自动生成——由服务端 canonical manifest 渲染，请勿手动编辑",
         f"FROM {spec.base_image}",
@@ -183,13 +187,13 @@ def render_dockerfile(spec: BuildSpec) -> str:
         "    PIP_NO_CACHE_DIR=1 \\",
         "    PIP_DISABLE_PIP_VERSION_CHECK=1",
         "# 平台固定运行依赖：ipykernel（Notebook）+ pytest（判题）",
-        f'RUN pip install --no-cache-dir "ipykernel=={IPYKERNEL_VERSION}" "pytest=={PYTEST_VERSION}"',
+        f'RUN pip install --no-cache-dir{_index_opt} "ipykernel=={IPYKERNEL_VERSION}" "pytest=={PYTEST_VERSION}"',
     ]
     pypi_pkgs = [p for p in spec.packages if p.source_key != "pytorch_cpu"]
     torch_pkgs = [p for p in spec.packages if p.source_key == "pytorch_cpu"]
     if pypi_pkgs:
         lines.append("# 档位教学包（精确版本锁定）")
-        lines.append("RUN pip install --no-cache-dir \\")
+        lines.append(f"RUN pip install --no-cache-dir{_index_opt} \\")
         for i, entry in enumerate(pypi_pkgs):
             sep = " \\" if i < len(pypi_pkgs) - 1 else ""
             lines.append(f"    {_pip_requirement(entry)}{sep}")
diff --git a/backend/docker/judge/Dockerfile b/backend/docker/judge/Dockerfile
index b6a41b6..8fe628e 100644
--- a/backend/docker/judge/Dockerfile
+++ b/backend/docker/judge/Dockerfile
@@ -1,7 +1,8 @@
 FROM python:3.12-slim
 
 ENV PYTHONDONTWRITEBYTECODE=1 \
-    PYTHONUNBUFFERED=1
+    PYTHONUNBUFFERED=1 \
+    PIP_INDEX_URL=https://pypi.tuna.tsinghua.edu.cn/simple
 
 RUN pip install --no-cache-dir pytest numpy pandas scikit-learn
 
diff --git a/backend/docker/kernel/Dockerfile b/backend/docker/kernel/Dockerfile
index d9879b1..5d9beca 100644
--- a/backend/docker/kernel/Dockerfile
+++ b/backend/docker/kernel/Dockerfile
@@ -2,7 +2,8 @@ FROM python:3.12-slim
 
 ENV PYTHONDONTWRITEBYTECODE=1 \
     PYTHONUNBUFFERED=1 \
-    PIP_NO_CACHE_DIR=1
+    PIP_NO_CACHE_DIR=1 \
+    PIP_INDEX_URL=https://pypi.tuna.tsinghua.edu.cn/simple
 
 # 安装 ipykernel + 常用数学/数据科学库
 RUN pip install --no-cache-dir \
