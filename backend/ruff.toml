# 后端高信号 lint 规则（TASK-026，与 CI 门禁一致）
#
# 仅阻断高信号类别：
#   E9   语法错误
#   F63  错误比较/表达式
#   F7   错误用法（永不成立的断言等）
#   F82  未定义名称
#   F811 重复定义
# 低信号风格规则刻意不启用，避免与历史代码冲突、引入噪音。
[lint]
select = ["E9", "F63", "F7", "F82", "F811"]
